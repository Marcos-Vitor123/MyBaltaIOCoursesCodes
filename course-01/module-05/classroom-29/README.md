# Operações de Comparação
- Sempre retorna **verdadeiro** ou **falso**
- Exemplos:
  - Igual `==` 
  - Diferente `!=`
  - Maior que `>`
  - Menor que `<`
  - Maior ou igual a `>=`
  - Menor ou igual que `<=`